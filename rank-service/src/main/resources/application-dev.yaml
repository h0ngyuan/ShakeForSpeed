server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfs_db?useSSL=false&serverTimezone=UTC
    username: root
    password: hyuans
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    database: 1

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

minio:
  endpoint: http://127.0.0.1:9000
  access-key: minioadmin
  secret-key: minioadmin
  buckets:
    reward-img: sfs-reward-img

jwt:



